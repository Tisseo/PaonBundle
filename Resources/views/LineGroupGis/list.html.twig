{% extends "TisseoCoreBundle::datatable_list.html.twig" %}
{% set bundle = "TisseoPaonBundle" %}

{% block list_title -%}
    {{ 'line_group_gis.title'|trans({}, 'default') }}
    {% if is_granted('BUSINESS_MANAGE_GROUP_GIS') %}
        -
        <a href="{{ path('tisseo_paon_line_group_gis_create') }}" data-toggle="modal" data-target="#base-modal" class="btn btn-success">
            <span class="glyphicon glyphicon-plus"></span> {{ 'line_group_gis.label.create'|trans({}, 'default') }}
        </a>
    {% endif %}
{% endblock %}

{% block table_head -%}
    <th class="col-md-2">{{ 'line_group_gis.list.columns.name'|trans({}, 'default') }}</th>
    <th class="col-md-7">{{ 'line_group_gis.list.columns.line'|trans({}, 'default') }}</th>
    <th class="col-md-3 no-search no-sort">{{ 'global.actions'|trans }}</th>
{% endblock %}

{% block table_body -%}
    {% for group in groups %}
        <tr>
            <td>
                {{ group.name }}
            </td>
            <td>
                <table class="table table-condensed">
                    <thead>
                        <th>{{ 'line_group_gis.list.columns.line_number'|trans({}, 'default') }}</th>
                        <th>{{ 'line_group_gis.list.columns.last_schematic'|trans({}, 'default') }}</th>
                        <th>{{ 'line_group_gis.list.columns.last_usable_schematic'|trans({}, 'default') }}</th>
                    </thead>
                    <tbody>
                        {% for line in group.lineGroupGisContents %}
                            {% set lastSchematic = line.line.LastSchematic(true) %}
                            {% set lastValidSchematic = line.line.LastValidSchematic %}
                            <tr>
                                <td>{{ line.line.number }}</td>
                                <td>{% if lastSchematic %}{{ lastSchematic.date|date('d/m/Y') }}{% else %}-{% endif %}</td>
                                <td>{% if lastValidSchematic %}{{ lastValidSchematic.date|date('d/m/Y') }}{% else %}-{% endif %}</td>
                            </tr>
                        {% else %}
                            <tr><td colspan="3">{{ 'global.no_items'|trans }}</td><tr>
                        {% endfor %}
                    </tbody>
                </table>
            </td>
            <td>
                {% if is_granted('BUSINESS_MANAGE_GROUP_GIS') %}
                <a class="btn btn-default" href="{{ path('tisseo_paon_line_group_gis_edit', {'lineGroupGisId': group.id }) }}" data-toggle="modal" data-target="#base-modal">
                    <span class="glyphicon glyphicon-edit"></span> {{ 'global.edit'|trans }}
                </a>
                <a class="btn btn-default" href="{{ path('tisseo_paon_line_group_gis_delete', {'lineGroupGisId': group.id }) }}">
                    <span class="glyphicon glyphicon-edit"></span> {{ 'global.delete'|trans }}
                </a>
                {% endif %}
            </td>
        </tr>
    {% endfor %}
{% endblock %}
