{% extends "TisseoDatawarehouseBundle::datatable_list.html.twig" %}

{% import "TisseoDatawarehouseBundle::macros.html.twig" as render %}

{% block modal_dialog_classes %}
    modal-dialog-large
{% endblock %}

{% block list_title -%}
    {{'calendar.list.title'|trans({}, 'default')}}
{% endblock %}

{% block table_head -%}
    <th>{{ 'line_version.list.columns.label'|trans({}, 'default') }}</th>
    <th>{{ 'line_version.list.columns.physical_mode'|trans({}, 'default') }}</th>
    <th>{{ 'line_version.list.columns.version'|trans({}, 'default') }}</th>
    {% if is_granted('BUSINESS_MANAGE_CALENDAR') %}
        <th class="no-sort">{{'global.actions'|trans({}, 'messages')}}</th>
    {% endif %}
{% endblock %}

{% block table_body -%}
    {% if lineVersions|length == 0 %}
        <tr>
            <td colspan="4">
                {{'global.no_items'|trans}}
            </td>
        </tr>
    {% else %}
        {% for lineVersion in lineVersions %}
            <tr>
                <td>
                    {{ render.line_number(lineVersion, 'lineVersion', 'small') }} <span>{{ lineVersion.name }}</span>
                </td>
                <td>
                    {{ lineVersion.line.physicalMode.name }}
                </td>
                <td>
                    {{ lineVersion.version }}
                </td>
                <td>
                    {% if is_granted('BUSINESS_MANAGE_CALENDAR') %}
                        <a class="btn btn-default calendar-edit" href="{{ path('tisseo_datawarehouse_calendar_edit', {'lineVersionId': lineVersion.id}) }}" data-toggle="modal" data-target="#base-modal">
                            <span class="glyphicon glyphicon-edit"></span> {{'global.edit_calendar'|trans({}, 'messages')}}
                        </a>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
    {% endif %}
{% endblock %}
