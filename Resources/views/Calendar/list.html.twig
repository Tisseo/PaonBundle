{% extends "TisseoDatawarehouseBundle::generic_list.html.twig" %}

{% block list_title -%}
    {{'calendar.list.title'|trans({}, 'default')}}
    {% if is_granted('BUSINESS_MANAGE_CALENDAR') %}
        -
        <a href="{{ path('tisseo_datawarehouse_calendar_edit') }}" data-toggle="modal" data-target="#base-modal" class="btn btn-success">
            <span class="glyphicon glyphicon-plus"></span> {{'calendar.create'|trans({}, 'default')}}
        </a>
    {% endif %}
{% endblock %}

{% block table_head -%}
    <th>{{'calendar.list.columns.label'|trans({}, 'default')}}</th>
    {% if is_granted('BUSINESS_MANAGE_CALENDAR') %}
        <th>{{'global.actions'|trans({}, 'messages')}}</th>
    {% endif %}
{% endblock %}

{% block table_body -%}
    {% if calendars|length == 0 %}
        <tr>
            <td colspan="4">
                {{'global.no_items'|trans}}
            </td>
        </tr>
    {% else %}
        {% for calendar in calendars %}
            <tr>
                <td>
                    {{calendar.number}}
                </td>
                <td>
                    {% if is_granted('BUSINESS_MANAGE_CALENDAR') %}                        
                        <a class="btn btn-default" href="{{ path('tisseo_datawarehouse_calendar_edit', {'calendarId': calendar.id}) }}" data-toggle="modal" data-target="#base-modal">
                            <span class="glyphicon glyphicon-edit"></span> {{'global.edit'|trans({}, 'messages')}}
                        </a>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
    {% endif %}
{% endblock %}
