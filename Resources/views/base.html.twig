{% extends "TisseoCoreBundle::base.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
    {% stylesheets 'bundles/tisseopaon/css/main.css'
                   filter='cssrewrite' output='css/paon.css' %}
        <link rel="stylesheet" href="{{ asset_url }}?{{ assets_version }}" />
    {% endstylesheets %}
{% endblock %}

{% block head_config %}
    {{ parent() }}
    {% include 'TisseoPaonBundle::config.js.html.twig' %}
{% endblock %}

{% block title %}
{{ parent() }}
- {{ 'tisseo.paon.application'|trans }}
    {% if pageTitle %}
        - 
        {% if titleParameters %}
            {{ pageTitle|trans(titleParameters) }}
        {% else %}
            {{ pageTitle|trans }}
        {% endif %}
    {% endif %}
{% endblock %}

{% block navbar_brand %}
    <a href="{{ path('tisseo_paon_homepage') }}">{{ 'tisseo.paon.application'|trans }}</a>
{% endblock %}

{% block root_breadcrumb %}
    <ol class="breadcrumb">
        <li>
        {% block breadcrumb %}
            {% if navTitle %}{{ navTitle|trans }}{% endif %}
        {% endblock %}
        </li>
    </ol>
{% endblock %}
